# ==== Core scientific stack (as pinned in your conda spec) ====
numpy==1.26.4                 # Fast N-dim arrays & vectorized math
pandas==2.3.3                 # DataFrames, CSV/Parquet I/O, time series
scipy==1.15.2                 # Optimization, linear algebra, stats, FFT

# ==== Graphs, algebra, and math helpers ====
networkx==3.4.2               # Graph structures/algorithms (paths, centrality)
sympy==1.14.0                 # Symbolic math (algebra/calculus; exact arithmetic)
mpmath==1.3.0                 # Arbitrary-precision floating-point computations

# ==== Jupyter / IPython runtime (for notebooks) ====
ipython==8.37.0               # Rich REPL, magics, pretty tracebacks
ipykernel==7.0.0              # Kernel so notebooks run Python
jupyter_client==8.6.3         # Comm layer between Jupyter frontends & kernels
jupyter_core==5.8.1           # Core Jupyter utilities & config
pyzmq==27.1.0                 # ZeroMQ bindings used by Jupyter messaging
tornado==6.5.2                # Async web server Jupyter relies on
traitlets==5.14.3             # Config system Jupyter/ipy* use
matplotlib-inline==0.1.7      # Inline Matplotlib backend in notebooks

# ==== Utilities & runtime glue ====
joblib==1.5.2                 # Lightweight parallelism & caching for Python/ML
psutil==7.1.0                 # System/process info (CPU/RAM) for monitoring
threadpoolctl==3.6.0          # Control/openMP threadpools (NumPy/BLAS interop)
python-dateutil==2.9.0.post0  # Robust datetime parsing
pytz==2025.2                  # Timezone definitions
typing_extensions==4.15.0     # Backports of typing features
setuptools==80.9.0            # Build/install helper for Python packages
wheel==0.45.1                 # Built-package (wheel) format support
pip==25.2                     # Python package installer
six==1.17.0                   # Py2/3 compatibility shims used by some deps
zipp==3.23.0                  # Zip file path helpers (used by importlib-metadata)
importlib-metadata==8.7.0     # Access package metadata at runtime

# ==== Deep learning backend (CPU/MKL build as in spec) ====
# Your conda spec pins the MKL CPU build of PyTorch:
torch==2.1.2                  # Installed as 'pytorch' (cpu + MKL) via conda
# torchvision is NOT present in your spec. Add if you need vision models:
# torchvision==0.16.2         # Vision datasets/models/transforms (optional)

# ==== Optional: TabPFN and friends (NOT present in the conda spec) ====
# TabPFN is typically installed via pip. Uncomment if you need it here:
# tabpfn==2.2.1               # Tabular Prior-Data Fitted Networks (v2 API)
# If you’ll do post-hoc ensembles or AutoTabPFN, add (optional):
# tabpfn-extensions==0.1.5    # Extra utilities/ensembles around TabPFN
# autogluon.tabular==1.1.1    # AutoML (tabular); heavy, optional

# ==== Optional plotting & stats (NOT present in the conda spec) ====
# matplotlib==3.7.2           # Core plotting
# seaborn==0.12.2             # Statistical plotting on top of Matplotlib
# plotly==5.17.0              # Interactive plotting
# statsmodels==0.14.0         # Classical stats/GLMs/time series
# scikit-learn==1.3.2         # Standard ML toolkit (estimators, metrics, CV)
# xgboost==1.7.6              # Gradient boosting trees (CPU/GPU)
# lightgbm==4.1.0             # Fast gradient boosting (CPU/GPU)
# catboost==1.2.2             # Gradient boosting with categorical support

# ==== Spreadsheet I/O (NOT present in the conda spec) ====
# openpyxl==3.1.2             # Excel .xlsx read/write
# xlrd==2.0.1                 # Legacy Excel .xls reader (limited)
